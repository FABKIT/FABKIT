# Umami website id (https://cloud.umami.is). Only read on production builds
# (MODE=production) — analytics is a no-op in dev and staging/fork builds
# regardless of whether this is set.
VITE_UMAMI_WEBSITE_ID=
